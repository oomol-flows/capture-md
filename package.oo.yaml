name: md_to_image
displayName: "%display-name%"
description: "%description%"
version: 0.0.1
scripts:
  bootstrap: |
    npm install
    npx playwright install chromium
    npx playwright install-deps chromium
    poetry install --no-root
dependencies:
  oomol-preview: 0.2.0
icon: ./icon.png
repository: https://github.com/oomol-flows/md_to_image
releaseNotes: init
